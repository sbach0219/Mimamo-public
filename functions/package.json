{
  "name": "thousand-sky-functions",
  "description": "Cloud Functions for Mimamo (push notifications)",
  "main": "index.js",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "test": "node --test",
    "deploy": "firebase deploy --only functions",
    "logs": "firebase functions:log"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^7.4.0"
  },
  "private": true
}
